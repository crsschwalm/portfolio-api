service: expressAPI-epsagon
provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-2
functions:
  server:
    handler: index.server
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'
